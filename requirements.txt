# WaveTechToolBox Dependencies

# Core Discord library - requires 2.12+ for DAVE (E2EE) voice support (Discord enforces 4017 otherwise)
# [voice] extra pulls in dave-py, PyNaCl, audioop-lts for voice connections
disnake[voice]>=2.12.0

# HTTP client for API calls (OpenAI TTS, DALL-E)
aiohttp>=3.9.0

# Environment variable management
python-dotenv>=1.0.0

# Voice encryption (required by disnake for voice features)
PyNaCl>=1.5.0

# Note: OpenAI API is accessed via HTTP (aiohttp), not the openai package
